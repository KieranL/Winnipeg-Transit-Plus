<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <CheckBoxPreference
        android:key="pref_disable_ads"
        android:title="@string/pref_title_disable_ads"
        android:defaultValue="false"
        android:widgetLayout="@layout/checkbox"/>
    <ListPreference
        android:key="@string/pref_key_theme"
        android:title="@string/pref_theme_title"
        android:defaultValue="0"
        android:entries="@array/pref_theme"
        android:entryValues="@array/pref_theme_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

    <ListPreference
        android:key="@string/pref_key_nearby_distance"
        android:title="@string/pref_nearby_distance_title"
        android:defaultValue="200"
        android:entries="@array/pref_nearby_distance"
        android:entryValues="@array/pref_nearby_distance_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
